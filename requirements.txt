# Versions this was developed and tested against (Python 3.11).
# Anything reasonably recent should work; these are lower bounds, not pins.
torch>=2.0
numpy>=1.24,<2
pandas>=2.0
h5py>=3.8
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
